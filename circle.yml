dependencies:
  override:
    - npm install -g npm
    - npm install --only=dev:
        timeout: 6000
    - bower install --dev:
        timeout: 6000
test:
  post:
    - codeclimate-test-reporter < coverage/lcov.info

